import React from 'react';

const Home = () => {
  return (
    <div>
      I'm the home component !!
      <button onClick={() => console.log('hello')}>Click me</button>
    </div>
    )
}

export default Home;
 